{
  "name": "@novalight/ai-core",
  "version": "0.0.0",
  "private": true,
  "description": "AI provider/prompt/agent/evaluation abstraction interfaces only -- no vendor SDK wired, no paid API calls. See src/index.ts.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint \"src/**/*.ts\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"no tests yet -- interfaces only, nothing to execute\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
