# Copy to .env (or .env.local) for local development. Never commit a real .env file.
# Per docs/architecture/012-database-architecture.md: PostgreSQL via Prisma, hosted on
# Supabase in staging/production (ADR-012); any local Postgres instance works for development.
DATABASE_URL=postgresql://user:password@localhost:5432/novalight?schema=public
